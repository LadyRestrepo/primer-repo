/*algunas funciones no tienen parametros pero tienen retorno, son usadas generalmente para formatear mensajes cambia estados de una varibale entre otros. retornar valores de constantes */
const PI = 3.1416;
function ValorPi() {
    return PI ;

    }
let resultadoPi = ValorPi() ;
console.log(resultadoPi);



