//las funciones permiten reciclar codigo, hacen que el codigo sea mucho mas legible
// f(x) = 2x + 1
function f(x) {
    let resultado = 2* x + 1;
    // el return marca el fin de la funcion y se encarga d devolver un valor funcional 
    return resultado
}
let returno1 = f(2)
console.log("Resultado de la funcion : ", returno1);
let retorno2 = f(5)
console.log("Resultado de la funcion : ", retorno2);

