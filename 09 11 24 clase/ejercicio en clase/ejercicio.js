//Función que recibe un arreglo de números como argumento y retorna su promedio
/*
function promedioArreglo(arreglo) {
  let  promedio = 0
    for (let i = 0; i < arreglo.length; i++) {
       promedio+= arreglo[i]
     } ;
      let  promedio = promedio / arreglo.length
       return promedio
    }
console.log(promedioArreglo([5, 0, 6, 8]));
*/

//Función que recibe un arreglo de números enteros y retorna un arreglo nuevo pero 
//solo con los números impares
/*function numimpar(arreglo) {
    let arregloNuevo = [];
    for (let index = 0; index < arreglo.length; index++) {
        if (arreglo[index] % 2 !== 0) {
            arregloNuevo.push(arreglo[index]);
        }

    }
    return arregloNuevo;
}
const arreglo = [12, 3, 4, 5, 7];
console.log(numimpar(arreglo));

*/


//Función que recibe un arreglo de números y retorna el mayor
/*
function mayor(arreglo) {
    let mayor = arreglo[0];
    for (let index = 0; index < arreglo.length; index++) {

        if (mayor < arreglo[index]) {
            mayor = arreglo[index];
        }

    }
    return mayor
}

let arreglo = [22, 700, -6, 88, 30];
console.log("mayor", mayor(arreglo));
*/

//Use indexOf para mostrar los índices de los elementos 44, 89, 70 del arreglo [30, 
// 44,54, 89, 100]

/*function buscarNombre(arreglo, nombre) {
    //usando indexOf podemos saber si el nombre
    //está ene l arreglo o no
}*/

function buscarNombre(arreglo, nombre) {




    //usando indexOf podemos saber si el nombre
    //está ene l arreglo o no